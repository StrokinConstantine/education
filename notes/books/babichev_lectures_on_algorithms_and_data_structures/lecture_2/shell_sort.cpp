void shellsort(int *a, int n) { int h; for (h = 1; h <= n / 9; h = 3*h + 1) ;
for ( ; h > 0; h /= 3) { for (int i = h; i < n; i++) {
	void shellsort(int *a, int n) { int h; for (h = 1; h <= n / 9; h = 3*h + 1) ;
for ( ; h > 0; h /= 3) { for (int i = h; i < n; i++) {